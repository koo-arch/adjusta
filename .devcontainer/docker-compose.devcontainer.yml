services:
  workspace:
    image: mcr.microsoft.com/devcontainers/base:ubuntu

    working_dir: /workspace

    volumes:
      - .:/workspace:cached

    command: sleep infinity

    networks:
      - app-network
